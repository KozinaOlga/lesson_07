'use strict';

/*
const getRandomNumber = (elem, n, m) => {
    const array = [];

    array.push(Math.trunc((Math.random() * 100) + 1));
    console.log('array: ', array);
}

getRandomNumber();

const generateRandomArray = (n, m, count) => {
    const result = [];
    const min = Math.min(n, m);
    const max = Math.max(n, m);
    let i = 0;
  
    while (i < count) {
      i++;
      const randomNum = Math.floor(Math.random() * (max - min + 1) + min);
      result.push(randomNum);
  
    }
    return result;
  }
  console.log(generateRandomArray(10, -50, 100));


  
  let count = 42;
  let n = -3;
  let m = -10;
  let a = [];
  let max = Math.max(n,m);
  let min = Math.min(n,m);
  for (let i = 0 ; i < count; i ++){
      a.push(Math.floor(Math.random() * (max - min + 1) + min));
  }
  
  console.log(a)
  */